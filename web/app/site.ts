// Canonical origin for metadata, robots.txt and sitemap.xml. The project has no
// custom domain, so this is the stable workers.dev hostname the Worker deploys
// to. Swap it the day a domain is attached.
export const SITE_URL = "https://perdido-peas-web-production.ian-b42.workers.dev";
