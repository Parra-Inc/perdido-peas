{
  "name": "perdido-peas-book",
  "version": "0.1.0",
  "private": true,
  "description": "Perdido Peas, a children's book by Ian MacCallum and Katie Rivas. Pages rendered with open-assets.",
  "scripts": {
    "dev": "concurrently \"npx @tailwindcss/cli -i src/styles.css -o dist/styles.css --watch\" \"npx open-assets dev\"",
    "build:css": "npx @tailwindcss/cli -i src/styles.css -o dist/styles.css",
    "render": "npm run build:css && npx open-assets render --force"
  },
  "devDependencies": {
    "@open-assets/open-assets": "^0.4.0",
    "@tailwindcss/cli": "^4.0.0",
    "concurrently": "^9.0.0",
    "tailwindcss": "^4.0.0"
  }
}
